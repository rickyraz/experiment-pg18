// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: query.sql

package experiment_pg18

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createProduct = `-- name: CreateProduct :one
INSERT INTO products_v18 (name, base_price, tax_rate, discount_pct)
VALUES ($1, $2, $3, $4)
RETURNING id, name, base_price, tax_rate, discount_pct, tax_amount, discount_amount, final_price, search_slug
`

type CreateProductParams struct {
	Name        string
	BasePrice   pgtype.Numeric
	TaxRate     pgtype.Numeric
	DiscountPct pgtype.Numeric
}

func (q *Queries) CreateProduct(ctx context.Context, arg CreateProductParams) (ProductsV18, error) {
	row := q.db.QueryRow(ctx, createProduct,
		arg.Name,
		arg.BasePrice,
		arg.TaxRate,
		arg.DiscountPct,
	)
	var i ProductsV18
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.BasePrice,
		&i.TaxRate,
		&i.DiscountPct,
		&i.TaxAmount,
		&i.DiscountAmount,
		&i.FinalPrice,
		&i.SearchSlug,
	)
	return i, err
}

const getProduct = `-- name: GetProduct :one

SELECT id, name, base_price, tax_rate, discount_pct, tax_amount, discount_amount, final_price, search_slug FROM products_v18 WHERE id = $1
`

// sama dengan gen_random_uuid()
func (q *Queries) GetProduct(ctx context.Context, id pgtype.UUID) (ProductsV18, error) {
	row := q.db.QueryRow(ctx, getProduct, id)
	var i ProductsV18
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.BasePrice,
		&i.TaxRate,
		&i.DiscountPct,
		&i.TaxAmount,
		&i.DiscountAmount,
		&i.FinalPrice,
		&i.SearchSlug,
	)
	return i, err
}

const listProducts = `-- name: ListProducts :many
SELECT id, name, base_price, tax_rate, discount_pct, tax_amount, discount_amount, final_price, search_slug FROM products_v18 ORDER BY id LIMIT $1
`

func (q *Queries) ListProducts(ctx context.Context, limit int32) ([]ProductsV18, error) {
	rows, err := q.db.Query(ctx, listProducts, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ProductsV18
	for rows.Next() {
		var i ProductsV18
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.BasePrice,
			&i.TaxRate,
			&i.DiscountPct,
			&i.TaxAmount,
			&i.DiscountAmount,
			&i.FinalPrice,
			&i.SearchSlug,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
